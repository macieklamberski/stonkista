{
  "name": "@stonkista/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch index.ts",
    "test": "bun test",
    "db:push": "drizzle-kit push --config database/config.ts",
    "db:generate": "drizzle-kit generate --config database/config.ts",
    "db:migrate": "drizzle-kit migrate --config database/config.ts"
  },
  "dependencies": {
    "bullmq": "^6.2.0",
    "drizzle-orm": "^1.0.0-rc.5-ab785fc",
    "hono": "^4.13.3",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "drizzle-kit": "^1.0.0-rc.5-ab785fc"
  }
}
